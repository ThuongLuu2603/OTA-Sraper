kind = "web"
previewPath = "/"
title = "Agoda Scraper"
version = "1.0.0"

[[services]]
localPort = 5000
name = "web"
paths = ["/"]

[services.development]
run = "cd agoda-scraper && streamlit run app.py --server.port 5000"
